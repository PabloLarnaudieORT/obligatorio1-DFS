$kind: http-request
name: /actualizarUsuarioProductoPorId
url: "{{productionURL}}/v1/usuario-productos/{{idUsuarioProductos}}"
method: PATCH
body:
  type: json
  content: "{\r

    \    \"estado\":\"finalizado\"\r

    }"
auth:
  type: bearer
  credentials:
    token: "{{token1}}"
order: 3000

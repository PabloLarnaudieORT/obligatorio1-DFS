$kind: http-request
name: actualizar usuario con admin
url: "{{baseURL}}/v1/usuarios/{{idUsuarioActualizar}}"
method: PATCH
body:
  type: json
  content: "{\r\n    \r\n}"
auth:
  credentials:
    token: "{{token}}"
  type: bearer
order: 10000
